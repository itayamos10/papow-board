# PapoW Board

A password-gated, read-only Streamlit viewer that renders pre-computed research snapshots stored
in a database. Contains no analysis or strategy logic. Demo/paper research only — not investment
advice; places no orders.
